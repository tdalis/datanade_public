import React from 'react';
import { Figure, Card, CardColumns } from 'react-bootstrap';
import styled from 'styled-components';
import { ServiceCards } from './serviceComponents/ServiceCards';

export const Services = () => (
    <React.Fragment>
        <ServiceCards />
    </React.Fragment>
)